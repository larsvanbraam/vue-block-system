import VueTypes from 'vue-types';

/**
 * @param {description} scrollId The unique id of the block, used for scrolling to blocks by hashes
 * @param {placeholder} scrollId unique-block-id
 */
export default {
	scrollId: VueTypes.string,
};
