/**
 * Enum that determines the type of a component
 * This sets the component type, we start at 10 so we do not overwrite the vue-transition-component component types
 */
enum BlockSystemComponentType {
	/**
	 * Block component
	 */
	BLOCK_COMPONENT,
	/**m
	 * Button component
	 */
	BUTTON_COMPONENT,
}

export default BlockSystemComponentType;
