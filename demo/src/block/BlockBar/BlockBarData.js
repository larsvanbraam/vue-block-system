import VueTypes from 'vue-types';

/**
 * @param {description} id This is the id of the component
 * @param {description} blocks This is the array of child blocks that can be showed within the blocks
 */
export default {
	blocks: VueTypes.array,
};
